{
  "name": "hyperbind",
  "version": "7.0.1",
  "description": "Create or update DOM elements by mapping query selectors to hypertext, plain text, attributes, classes, properties, elements and lists.",
  "scripts": {
    "test": "ecstatic --cache no-cache --port 7357 test",
    "example": "ecstatic --cache no-cache --port 8080 example"
  },
  "devDependencies": {
    "ecstatic": "^4.1.4",
    "tap-esm": "^1.1.0"
  },
  "repository": "https://github.com/jessetane/hyperbind",
  "author": "jesse.tane@gmail.com",
  "license": "MIT"
}
