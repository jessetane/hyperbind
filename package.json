{
  "name": "hyperbind",
  "version": "4.0.1",
  "description": "Create or update DOM elements by mapping query selectors to hypertext, plain text, attributes, classes, properties, elements and lists.",
  "scripts": {
    "test": "cd test; watchify index.js -o build.js & ecstatic --port 7357 .",
    "example": "cd example; watchify index.js -o build.js & ecstatic --port 8080 ."
  },
  "devDependencies": {
    "ecstatic": "^2.2.1",
    "tape": "^4.6.3",
    "watchify": "^3.9.0"
  },
  "repository": "https://github.com/jessetane/hyperbind",
  "author": "jesse.tane@gmail.com",
  "license": "MIT"
}
